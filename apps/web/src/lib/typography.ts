/**
 * Shared heading scale for marketing sections (SectionIntro h2) and equivalent
 * page titles (blog index, post, legal). Hero uses a larger scale in HeroSection.
 */
export const sectionHeadingClass =
  'font-display text-xl font-bold tracking-tight text-slate-900 sm:text-2xl md:text-3xl lg:text-4xl dark:text-white'

/** Same type scale on solid dark / gradient bands (e.g. CTA). */
export const sectionHeadingClassOnDark =
  'font-display text-xl font-bold tracking-tight text-white sm:text-2xl md:text-3xl lg:text-4xl'
