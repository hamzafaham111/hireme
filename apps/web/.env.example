# Hire Me API (include /api/v1)
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1

# Copy for the header before the user sets an area (default: “Set your area”)
# NEXT_PUBLIC_LOCATION_UNSET_LABEL=Set your area

# Reverse geocode uses Photon (https://photon.komoot.io) — no API key.
