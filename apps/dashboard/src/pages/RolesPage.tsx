/**
 * Roles catalog temporarily disabled — no `/roles` API calls on this route.
 * Restore data loading here when the roles UI is needed again.
 */
export function RolesPage() {
  return <div className="min-h-[40vh]" aria-label="Roles" />
}
